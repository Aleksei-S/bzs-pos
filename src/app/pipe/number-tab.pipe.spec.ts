import { NumberTabPipe } from './number-tab.pipe';

describe('NumberTabPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberTabPipe();
    expect(pipe).toBeTruthy();
  });
});
